name: windows_basic

collectors:
  - type: filesystem_snapshot
    options:
      targets: "{{TARGETS}}"
      include_hidden: false
      hash_files: false
      exclude_globs:
        - "**/.git/**"
        - "**/node_modules/**"

report:
  format:
    - md